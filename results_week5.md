# Week 5 Task Set A — Error Analysis: Results

*Generated by `python rag.py eval-errors` at 2026-08-25T14:56:34+00:00. The 20 traces below
are a real, seeded random sample run through the pipeline exactly as it ships today; the
open-coding notes and the taxonomy grouping are hand-written in `eval/week5_open_coding.yaml`,
cross-checked against every trace by `validate_open_coding` before this report is written.*

| Setting | Value |
|---|---|
| Chunking strategy | `structure-aware` |
| Retrieval mode | `hybrid` (the shipped default, not pinned for this week) |
| Generation backend | `extractive` |
| Index namespace | `structure-aware__cs800_ov120` |
| Sample size / pool size / seed | 20 / 84 / 5 |

## 0. Sampling: random, not curated

`eval/week5_questions.yaml` is a pool of 84 candidate questions meant to stand in for real
traffic, not for a nice demo: one item per troubleshooting-table error code across all six
new articles (51, none hand-picked), plus prose-fact questions over both the new and legacy
articles, colloquial paraphrases, genuinely out-of-corpus questions, and deliberately hard
ambiguous or multi-part questions. The 20 traces below are
`random.Random(5).sample(pool, 20)` — a seed fixed *before* the sampler was ever run and never
re-rolled after seeing the result. Pool composition vs. what got drawn:

| Category | In pool | In this sample |
|---|---|---|
| `error_code_ticket` | 51 | 12 |
| `policy_prose` | 11 | 3 |
| `colloquial_paraphrase` | 8 | 2 |
| `out_of_corpus` | 8 | 2 |
| `multi_part_ambiguous` | 6 | 1 |

## 1. Every trace, read and coded

One honest note per trace, written from that trace alone before any grouping happened —
`eval/week5_open_coding.yaml` carries the full text; this table is the compressed view.

| id | category | outcome | severity | note |
|---|---|---|---|---|
| `M2` | multi_part_ambiguous | refused (false, partial) | 3 | The ERR-4032 half of this two-part question is answerable -- its chunk is in the top-5 -- but the gate scores the whole question as one unit, and the unanswerable "refund for the inconvenience" half drags coverage down to 29% (missing: wants, refund, inconvenience), so the customer gets a flat refusal instead of the ERR-4032 fix plus an honest "I can't speak to a refund" on the second half. |
| `E-4156` | error_code_ticket | answered (correct) | 0 | Same terse "X -- can you tell me what this means" template as E-4151 (coverage 100%); correct, precisely cited answer. |
| `E-4203` | error_code_ticket | refused (false) | 3 | Identical mechanism and phrasing to E-4088 (coverage 37%, same five missing filler words) -- a second, independent confirmation that this exact Week-4-style template is refused end-to-end under the currently shipped pipeline. |
| `C6` | colloquial_paraphrase | refused (defensible) | 2 | Correctly declines to guess which of several webhook error codes applies to this genuinely ambiguous ticket (coverage 40%, missing: stuff, stopped, idea) -- but gives a flat refusal instead of asking which error code or symptom the customer actually saw, even though four different HC-4006 webhook rows were sitting in the top-5. |
| `E-4004` | error_code_ticket | refused (false) | 3 | The ERR-4004 row is retrieved at rank 1 -- retrieval is not the problem here -- but the gate still refuses (coverage 51%, missing: getting, idea) because those two ordinary words are absent from this small corpus and not on the interrogative stopword list, so they inflate the missing-idf share past the 55% threshold. |
| `P9` | policy_prose | answered (correct, padded) | 1 | Correct primary fact ("30 days", coverage 100%), but the extractive generator pads the answer with an unrelated ERR-4204 table row the question never asked about, because that row scored just above the relative-cutoff threshold used to select supporting evidence. |
| `E-4155` | error_code_ticket | refused (false) | 3 | Same "trying / info / going" pattern as E-4002/E-4007 (coverage 48%); refused regardless of retrieval quality. |
| `E-4007` | error_code_ticket | refused (false) | 3 | Same "trying / info / going" filler pattern as E-4002 (coverage 48%); the ERR-4007 row is visibly retrieved at rank 3, but the pipeline refuses anyway. |
| `E-4088` | error_code_ticket | refused (false) | 3 | This is the exact "Support ticket: account throwing X, need root cause and remediation steps quickly" phrasing Week 4's K-series used to prove hybrid search fixes retrieval for this corpus. Hybrid does put an HC-4003 chunk with the ERR-4088 row in the top-5 here -- but the full pipeline still refuses end-to-end (coverage 37%, missing: throwing, root, remediation, steps, quickly), because the evidence gate is a second, independent gate Week 4's harness never ran this template through. |
| `E-4036` | error_code_ticket | refused (false) | 3 | The ERR-4036 row is retrieved at rank 1, but coverage is 45% (missing: hey, mentions, resolution) -- a casual "Hey, a customer's ticket mentions X" opener is enough on its own to sink an otherwise perfectly-retrieved question. |
| `E-4205` | error_code_ticket | answered (correct) | 0 | Same terse template as E-4151/E-4156 (coverage 100%); correct, precisely cited answer. |
| `P10` | policy_prose | answered (correct) | 0 | Correct, three-sentence, fully-cited answer pulling from both the legacy invoice article and the migration article (coverage 78%) -- a genuine prose-question success, not just a table-row lookup. |
| `O8` | out_of_corpus | refused (correct) | 0 | Correctly refused (coverage 51%, missing: compare, market) -- the corpus documents this company's own migration mechanics only and never mentions competitor pricing. |
| `E-4206` | error_code_ticket | refused (false) | 3 | Same "Getting X ... any idea why" template and same "getting / idea" filler pattern as E-4004/E-4035 (coverage 51%); the HC-4006 troubleshooting chunk with the ERR-4206 row is retrieved at rank 3, and the pipeline still refuses. |
| `C8` | colloquial_paraphrase | answered (wrong topic) | 3 | Retrieval drifted to the legacy invoice-period article (HC-1007) instead of HC-4003's ERR-4093 currency-conversion row, because both share generic "billing period" vocabulary; the extractive generator then confidently cited three sentences about invoice-period anchoring that never mention currency at all -- a fully grounded, citation-valid answer that is nonetheless about the wrong thing, with nothing to flag it as such. |
| `E-4035` | error_code_ticket | refused (false) | 3 | Coverage 51% (missing: getting, idea) -- same "Getting X ... any idea why" template as E-4004/E-4206, refused for the same filler-word reason regardless of what retrieval did. |
| `O2` | out_of_corpus | refused (correct) | 0 | Correctly refused (coverage 46%, missing: hotline, operation) -- no article covers support-hotline hours; this is the evidence gate working exactly as intended. |
| `E-4002` | error_code_ticket | refused (false) | 3 | Coverage 47% (missing: trying, info, going) refuses a plain "what's going on and how do we fix it" ticket even though an HC-4001 troubleshooting-table chunk is in the top-5. |
| `E-4151` | error_code_ticket | answered (correct) | 0 | Terse phrasing ("ERR-4151 -- can you tell me what this means and what I should do?") scores full coverage (100%) because its filler words happen to sit on the QUESTION_WORDS stoplist ("tell", "mean", "need"); answer is correct, terse, and precisely cited. |
| `P2` | policy_prose | answered (correct) | 0 | Correct, terse answer (coverage 57%); the exact decommission date is pulled straight from the phase table and correctly cited. |

**7/20 traces had no issue** — 7 answered, 13 refused, out of 20 total.

## 2. Named problems, ranked by frequency x severity

Severity: 3 = severe (a good answer existed and the user did not get it, or got a wrong one
with no signal it might be wrong), 2 = moderate (defensible but a better system would do
more), 1 = cosmetic (correct outcome, quality nit).

| Rank | Problem | Count | Mean severity | Score (count x severity) | Example traces |
|---|---|---|---|---|---|
| 1 | **False refusal on answerable questions (informal phrasing)** | 10 | 3.0 | 30.0 | `E-4002`, `E-4004`, `E-4007`, `E-4035`, `E-4036`, `E-4088`, +4 more |
| 2 | **Off-topic confident answer after retrieval drift** | 1 | 3.0 | 3.0 | `C8` |
| 3 | **Ambiguous question refused instead of clarified** | 1 | 2.0 | 2.0 | `C6` |
| 4 | **Correct answer padded with a tangential citation** | 1 | 1.0 | 1.0 | `P9` |

**False refusal on answerable questions (informal phrasing)** (`false_refusal_informal_phrasing`)

The evidence gate's idf-weighted coverage check treats ordinary support-ticket filler words ("getting", "trying", "throwing", "hey", "idea", ...) as missing corpus content, because they are absent from this small corpus and are not on the QUESTION_WORDS stoplist. A short, informally-phrased ticket can lose enough idf-weighted coverage to this filler alone that it is refused even when the exact answer-bearing chunk was retrieved at rank 1.

Traces: `E-4002`, `E-4004`, `E-4007`, `E-4035`, `E-4036`, `E-4088`, `E-4155`, `E-4203`, `E-4206`, `M2`

**Off-topic confident answer after retrieval drift** (`offtopic_confident_answer`)

When retrieval drifts to a chunk that shares surface vocabulary with the question but not its actual topic, the extractive generator can still find enough term overlap to select and cite sentences, producing a fully grounded, citation-valid answer that is nonetheless about the wrong thing, with no signal to the reader that it might be off-topic.

Traces: `C8`

**Ambiguous question refused instead of clarified** (`ambiguous_no_clarifying_question`)

A question that is genuinely ambiguous across more than one part of the corpus is refused outright with the standard refusal message, rather than being met with a clarifying question that names the specific possibilities the retrieved chunks already surfaced.

Traces: `C6`

**Correct answer padded with a tangential citation** (`tangential_citation_padding`)

The extractive generator's relative-cutoff unit selection can admit a sentence that scores just above the cutoff but is not substantively related to the question, appending it to an otherwise correct and sufficient answer.

Traces: `P9`

## Analysis

### What reading 20 real traces found that the dashboards did not

Week 3 and Week 4 both scored the app on question sets built to demonstrate a specific,
already-suspected mechanism (does a chunker preserve a table row; does hybrid search beat
semantic-only on keyword-diluted queries). That is the right tool for proving a fix works, but
it cannot surface a problem nobody has hypothesised yet, because every question in those sets
was chosen *because* it was expected to behave a particular way.

This week's sample was built to avoid that: `eval/week5_questions.yaml` treats "every
troubleshooting-table error code across the whole corpus, in a rotation of five realistic
support-ticket phrasings, plus prose facts, colloquial paraphrases, out-of-corpus questions and
deliberately hard cases" as the traffic distribution, and draws 20 of those 84 candidates with
`random.Random(5).sample(...)` — a seed fixed before the draw, not re-rolled after seeing the
result. What came back was not the failure mode either of the first two weeks anticipated.

### The headline: refusal rate the corpus does not justify

7 of the 20 sampled questions were answered; 13 were refused. Only 2 of those 13 refusals
(`O2`, `O8`) were on questions the corpus genuinely cannot answer — the pool's overall
out-of-corpus share is 8/84 (~10%), so a random sample of 20 would be expected to draw roughly
2 unanswerable questions, which is exactly what happened. The other 11 refusals were on
questions the corpus *does* answer: 9 straightforward error-code lookups (`E-4002`, `E-4004`,
`E-4007`, `E-4035`, `E-4036`, `E-4088`, `E-4155`, `E-4203`, `E-4206`), the two-part `M2`, and —
worth calling out on its own — `C8`, which did not refuse but answered confidently from the
wrong article entirely.

Reading the traces individually, not just the pass/fail count, is what made the mechanism
visible. In `E-4004`, `E-4036`, and `E-4206` the correct chunk is retrieved at rank 1 — this is
not a retrieval problem at all — and the pipeline refuses anyway. The evidence gate's
refusal reason names the culprit directly every time: `missing_terms` is never a real gap in
the corpus, it is a handful of ordinary conversational words the question happened to use —
*getting, idea, trying, info, going, hey, mentions, resolution, throwing, root, remediation,
steps, quickly*. None of those are in `grounding.py`'s `QUESTION_WORDS` stoplist, none of them
occur anywhere in this ~46-chunk corpus, so each one is assigned the *maximum* idf weight the
corpus can produce — and a short question built mostly out of boilerplate phrasing can lose
more than half its idf-weighted mass to words that were never meant to carry information in the
first place.

The five support-ticket templates split cleanly on this axis. Every question built from
*"{code} — can you tell me what this means and what I should do?"* passed the gate (`E-4151`,
`E-4156`, `E-4205`, 3/3) — not because it is any more informative than the others, but because
its filler words ("tell", "mean", "need") happen to already be on the stoplist. Every question
built from the other four templates failed (9/9): *"Support ticket: account throwing X..."*,
*"Customer says they got X when trying to update..."*, *"Getting X ... any idea why..."*, and
*"Hey, a customer's ticket mentions X..."*. That is a coincidence of which words a hand-written
stoplist happened to include, not a real difference in how answerable the underlying question
is — and it means the gate's behaviour on any new phrasing is close to unpredictable.

This also lands directly on a specific, checkable claim from Week 4: `E-4088` and `E-4203`
use the *exact* "Support ticket: account throwing X, need root cause and remediation steps
quickly" phrasing that `eval/week4_questions.yaml`'s K-series used to prove hybrid search fixes
retrieval on this corpus. It still does — the answer-bearing chunk is in the top-5 for both.
But Week 4's harness measured that "before" state under semantic-only retrieval and never ran
the resulting hybrid-search "after" state through the full pipeline for this template, so it
never noticed that a second, independent gate — the evidence gate, not the retriever — refuses
this exact phrasing regardless of which retrieval mode found the right chunk. Week 4's fix was
real and correctly measured on its own terms; it just was not the last thing standing between
this template and a working answer.

### A second, rarer, more dangerous pattern

`C8` asked a colloquial paraphrase of the currency-conversion question (the underlying fact is
`ERR-4093`; the pool's direct, formally-phrased version of the same fact, `E-4093`, exists but
was not drawn into this particular sample). Retrieval drifted to the legacy invoice article (`HC-1007`),
which shares generic "billing period" vocabulary with the question but has nothing to do with
currency. The evidence gate passed it (57% coverage — enough generic overlap to clear the bar),
and the extractive generator, scoring sentences by raw term overlap, picked three grounded,
correctly-cited sentences about invoice-period anchoring and presented them as the answer. The
result is indistinguishable in form from a correct answer: full citations, resolved chunk ids,
no refusal — and it is about the wrong thing. This happened once in 20 traces, far less often
than the refusal pattern, but it is the more dangerous failure mode of the two: a refusal costs
a customer a wasted message; a wrong-but-confident answer can send an agent down the wrong
remediation path without any signal to double-check it.

### Named problems, ranked

See the taxonomy table above (`results_week5.md` §2) for the counts; in order:

1. **False refusal on answerable questions (informal phrasing)** — 10/20 traces, severity 3,
   score 30. By far the largest and most severe group: it is the majority outcome for the
   error-code-lookup traffic this app almost certainly sees the most of in practice.
2. **Off-topic confident answer after retrieval drift** — 1/20, severity 3, score 3. Rare in
   this sample, but the single worst individual outcome observed, and worth flagging even
   though the frequency x severity score ranks it below problem 1 — a purely mechanical
   ranking cannot distinguish "rare and merely annoying" from "rare and actively misleading",
   and this is the latter.
3. **Ambiguous question refused instead of clarified** — 1/20, severity 2, score 2.
4. **Correct answer padded with a tangential citation** — 1/20, severity 1, score 1.

### The one problem to attack next: false refusals on informal phrasing

Frequency x severity puts this first, and it would still be first on judgment alone: it is
half of everything sampled, it is severe (the corpus has the answer and the customer does not
get it), and unlike problem 2 it is *systematic* rather than a one-off — the template split
(9/9 fail, 3/3 pass) shows this is not noise, it is a predictable property of which ordinary
words a phrasing happens to contain.

The mechanism is now specific enough to name a fix without guessing: `QUESTION_WORDS` in
`grounding.py` is a fixed, hand-written stoplist of interrogative scaffolding, and it was
calibrated against Week 3's eight formally-phrased questions, which never needed words like
"getting" or "trying" filtered out because they were not written in that register. It was
never re-checked against realistic support-ticket phrasing before this week.

**Prediction, to be verified empirically before it is shipped, not assumed:** widening the
filler-word handling in the evidence gate (either extending `QUESTION_WORDS` with the
conversational filler this sample actually surfaced, or making the coverage calculation
robust to a small number of unmatched terms rather than penalising every one at full idf
weight) should recover most or all of the 10 false refusals in this taxonomy group, without
reducing the true refusal rate on `O2`/`O8`-style out-of-corpus questions, and without
increasing `C8`-style wrong-topic answers (a coverage fix does not touch retrieval or
generation, so it should not be able to make problem 2 worse). The falsifiable check for next
week is the same one Week 4 used: re-run `python rag.py eval-errors` (or a fresh seeded sample)
before and after the change and report the before/after count on this exact taxonomy group,
plus a regression check that `eval/questions.yaml`'s three out-of-corpus questions are still
refused. This is deliberately left unimplemented this week — Week 5's task is the taxonomy and
the target, not the fix.

---

Raw artefacts: `results/week5_traces.json`, `results/week5_traces/*.md` (one file per trace),
`eval/week5_questions.yaml` (the pool), `eval/week5_open_coding.yaml` (the notes).
