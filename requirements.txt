# Core retrieval stack (all offline, deterministic)
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
PyYAML>=6.0

# Optional web UI
Flask>=3.0
streamlit>=1.30

# Optional grounded generation with Claude. Without credentials the pipeline
# falls back to the deterministic extractive generator and still runs end to end.
anthropic>=0.121
